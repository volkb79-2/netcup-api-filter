# Deprecated / historical documentation

These files are **archived for historical context only**. They are point-in-time review
reports, migration write-ups, status snapshots, or guides for an earlier architecture
(`config.yaml`, `client_id:secret_key` tokens, `generate_token.py`, a `/client` portal, the
action-based `POST /api` passthrough, `*_modern.html` templates) that no longer exists.

**Do not update these and do not treat them as current.** The living, in-sync documentation
is in the parent [`docs/`](../README.md) folder and the repository root
([`AGENTS.md`](../../AGENTS.md), [`README.md`](../../README.md), [`CHANGELOG.md`](../../CHANGELOG.md)).

If something here still has value, fold the accurate parts into the relevant active guide and
leave the archive copy untouched.
