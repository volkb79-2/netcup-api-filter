# Development and Testing Dependencies
# Includes all production dependencies plus tooling for local development

# Production dependencies (from requirements.webhosting.txt)
-r requirements.webhosting.txt

# Testing frameworks
pytest>=9.0
pytest-asyncio>=1.3
pytest-cov>=6.0  # Coverage reporting (term-missing + xml for CI)
hypothesis>=6.100  # Property-based testing (parsers/validators) — see TESTING_LESSONS_LEARNED §5

# WSGI server for local testing
gunicorn>=21.0.0

websockets>=15.0  # Ensure WebSocket support to access playwright standalone service

# Live testing dependencies
dnspython>=2.4.0  # DNS verification tests

# ---------------------------------------------------------------------------
# Mutation testing (manual/local only — see tooling/mutation/)
# ---------------------------------------------------------------------------
# Run via:  ./tooling/mutation/run.sh run [module-path]
# NEVER invoke from CI or pytest.ini.
#
# mutmut 3.x installs cleanly on Python 3.14 (the dev venv) via libcst cp314.
# It reads [mutmut] from setup.cfg at the project root (not pytest.ini).
mutmut==3.6.0